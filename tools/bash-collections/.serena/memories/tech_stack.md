# Tech Stack
The primary tech stack consists of:
- Bash (and standard Linux util commands like `awk`, `sed`, `grep`, `find`)
- Docker, Docker Compose
- Kubernetes (`kubectl`)
- AWS CLI
- Database utilities (PostgreSQL, MySQL, Redis, MongoDB)
- Networking tools (`socat`, `ufw`)