# Completion Guidelines
When a task is completed, ensure the following checklist is met:
- The script has a fully completed `README.md` with usage instructions.
- The script has detailed comments at the top (purpose, requirements).
- Error handling and logging are properly implemented.
- Tested locally if the environment permits.
- Verify that `chmod +x` instructions run successfully before deploying.